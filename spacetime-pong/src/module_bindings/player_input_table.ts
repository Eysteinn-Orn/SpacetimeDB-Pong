// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  DbContext,
  ErrorContextInterface,
  Event,
  EventContextInterface,
  Identity,
  ProductType,
  ProductTypeElement,
  ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from "@clockworklabs/spacetimedb-sdk";
import { PlayerInput } from "./player_input_type";
import { EventContext, Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `player_input`.
 *
 * Obtain a handle from the [`playerInput`] property on [`RemoteTables`],
 * like `ctx.db.playerInput`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.playerInput.on_insert(...)`.
 */
export class PlayerInputTableHandle {
  tableCache: TableCache<PlayerInput>;

  constructor(tableCache: TableCache<PlayerInput>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<PlayerInput> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `player_id` unique index on the table `player_input`,
   * which allows point queries on the field of the same name
   * via the [`PlayerInputPlayerIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.playerInput.player_id().find(...)`.
   *
   * Get a handle on the `player_id` unique index on the table `player_input`.
   */
  player_id = {
    // Find the subscribed row whose `player_id` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: Identity): PlayerInput | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.player_id, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: PlayerInput) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: PlayerInput) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: PlayerInput) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: PlayerInput) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: PlayerInput, newRow: PlayerInput) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: PlayerInput, newRow: PlayerInput) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
